services:
  test-db:
    image: postgres:17
    # env_file: .env.test
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: split_well_informed_test
    ports:
      - "5433:5432" # Map to 5433 to avoid conflict with your dev DB